import { Router, Request, Response } from 'express';

const router = Router();

router.post("/register");
router.post("/login");
router.get("/me", );
router.post("/playlist/:id", );

export default router